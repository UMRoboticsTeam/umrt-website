---
import Hero from './Hero.astro';

interface Props {
  title: string;
  description?: string;
  image?: string;
}

const {
  title,
  description,
  image,
} = Astro.props;
---

<Hero
  title={title}
  subtitle={description}
  imgSrc={image}
/>